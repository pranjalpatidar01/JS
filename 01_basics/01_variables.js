const accountId=1;
let accountName="Pranjal";
var accountPassword="1234"; //var is not used because of issue in block scope and function scope
accountCity="Jaipur"  // don't prefer this way 

let name; //undefined

console.log(accountId);
console.table([accountId,accountName,accountPassword]);